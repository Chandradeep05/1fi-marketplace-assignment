{
  "name": "1fi-marketplace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "mobile": "npm --workspace=apps/mobile start",
    "test:contracts": "npm --workspace=packages/contracts run typecheck"
  }
}
